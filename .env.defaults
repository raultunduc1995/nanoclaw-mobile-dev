# This file contains default values for environment variables. 
# You can override these by creating a .env file in the root of the project with the same keys.
# This is useful for feature flags and other non-secret configuration that you want to be able to easily change without modifying the codebase.
ASSISTANT_NAME="Andy"
# Feature flags — override these in .env to enable
ENABLE_WHATSAPP=false
